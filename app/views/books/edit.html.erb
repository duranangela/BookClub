<section>
  <% if current_user && current_admin? %>
    <h2 class='title'>Edit a Book by <%= @author.name_switch %></h2>
    <%= render partial: 'form' %>
  <% else %>
      <%= render file:'/public/404' %>
  <% end %>
</section>
